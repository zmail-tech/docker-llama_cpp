services:
  llama_cpp:
    image: ghcr.io/ggml-org/llama.cpp:${IMAGE_TAG}
    container_name: llama_cpp
    runtime: nvidia
    ports:
      - "${PORT}:8000"
    volumes:
      - ./models:/models
      - ./models.ini:/app/models.ini
    command: >
      --host 0.0.0.0
      --port 8000
      --models-preset /app/models.ini
      --models-autoload
      --models-max 1
      -ngl 99
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - LLAMA_API_KEY=${LLAMA_API_KEY}
    restart: unless-stopped
